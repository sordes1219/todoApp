.container
  %ul
  - @board.errors.full_messages.each do |message|
    %li= message
  = form_with(model: @board,local: true) do |f|
    %div
      = f.label :title, 'Name'
    %div
      = f.text_field :title
    %div
      = f.label :description, 'Description'
    %div
      = f.text_area :descripttion
    = f.submit 'Submit', class:'primary-btn'